language: cpp

compiler:
  - gcc
  - clang

before_install:
  - echo $LANG
  - echo $LC_ALL
  - if [ $TRAVIS_OS_NAME == linux ]; then sudo apt-get update && sudo apt-get install -y opencl-headers; fi
  #- if [ $TRAVIS_OS_NAME == osx ]; then brew update && brew install llvm35 && brew link --force llvm35; fi

#before_script:
  #- travis_retry bundle
  #- if [ $TRAVIS_OS_NAME == linux ]; then travis_retry ./configure --llvm-config llvm-config-3.4; fi
  #- if [ $TRAVIS_OS_NAME == osx ]; then travis_retry ./configure; fi

script:
  - cmake --build
  - ./bin/test_gcvl

#after_success:

branches:
  only:
    - master

notifications:
  email: false
  #webhooks:
  #  urls:
  #    - https://webhooks.gitter.im/e/9502afd22ca6c8e85fb3
  #  on_success: change
  #  on_failure: always
  #  on_start: always

os:
  - linux
  - osx
